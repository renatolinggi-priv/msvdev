[PHP]
display_errors = stderr

; Ziel fuer error_log() und PHP-Warnungen. Bewusst AUSSERHALB des Docroots,
; sonst waere das Log per Browser abrufbar. Ohne diese Zeile verschwinden alle
; error_log()-Meldungen (u.a. die geschluckten Exceptions in cron/check_benachrichtigungen.php).
; Aenderungen greifen erst nach Ablauf von user_ini.cache_ttl (Standard 300 s).
error_log = /home/bdebbd4/php_error.log

